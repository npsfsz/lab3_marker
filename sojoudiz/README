Zahra Sojoudi, 1000061826, zahra.sojoudi@mail.utoronto.ca

------------------------
Part1 : generateQRcode
------------------------
This part generates the proper QRcode given the issuer, accountName, and the secret.
In order to create the QR code, the issuer, accountName, and the secret are encoded and specially formatted.
To encode the secret, the secret is converted to a byte array and then encoded using the base32_encoder.
The proper format of the URI for HOTP and TOTP are as follows:
HOTP: otpauth://hotp/ACCOUNTNAME?issuer=ISSUER&secret=SECRET&counter=1
TOTP: otpauth://totp/ACCOUNTNAME?issuer=ISSUER&secret=SECRET&period=30

To run the program: ./generateQRcode [issuer] [accountName] [secretHex]


------------------------
Part1 : validateQRcode
------------------------
This part checks the HOTP and TOTP values it receives from the user.
In this part the values for HOTP and TOTP are calculated using the secret it receives from the user and the counter and timer values.
The secret is used to create the HMAC value. HMAC = SHA((opad xor secret) || SHA(opad xor secret)||message)).
The message for HOTP is the byte array of the counter and for the TOTP is the byte array of the timer.
After the values are calculated, they are compared to the values provided by the user.

To run the program: ./validateQRcode [secretHex] [HOTP] [TOTP]
