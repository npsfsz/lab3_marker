#Jiayuan Chen, 1001401716, jiayuan.chen@mail.utoronto.ca
#Feiyu Ren, 1001128344, feiyu.ren@mail.utoronto.ca

GenerateQRcode.c:
We first encode issuer and account name using the provided urlEncode() function, then we need to convert secret_hex from string to hex format, then encode the converted hex array with base32_encode(), the last step is just to use snprintf to display the message while saving it to a buffer, which is used to display the QR code.


ValidateQRcode.c:
To validate, we first calculate the HMAC of the generated token using the secret, then follow the equation provided in rfc4226.pdf sec 5.4 namely 

	int offset = HMAC_val[19] & 0xF;

	int binary = (HMAC_val[offset] & 0x7f) << 24
	| (HMAC_val[offset+1] & 0xff) << 16
	| (HMAC_val[offset+2] & 0xff) << 8
	| (HMAC_val[offset+3] & 0xff);

which is same approach taught in lecture.

