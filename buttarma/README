#Armaan Butt, 999584560, armaan.butt@mail.utoronto.ca
#Aaron Baluja, 999620405, aaron.baluja@mail.utoronto.ca

Part 1: Generating an otpauth:// URI

The generateQRcode uses the ISSUER, ACCOUNTNAME and SECRET to generate a link 
and QR code based on the specifications provided. The secret key is an 80-bit 
value. Thus the input hex key string should be less than 20 characters.

$ ./generateQRcode <ISSUER> <ACCOUNTNAME> <SECRET>

Part 2: Validating the Codes

The validateQRcode uses the SECRET, HOTP and TOTP for validation. The input hex key
string must be less than 20 characters. The HOTP and TOTP values must be 6 charact-
ers each. 

$ ./validateQRcode <SECRET> <HOTP> <TOTP>




